{
  "name": "@agent-media/mcp",
  "version": "0.0.2",
  "description": "MCP adapter for Agent Media.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "dependencies": {
    "@agent-media/core": "workspace:*",
    "@agent-media/ffmpeg": "workspace:*"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "typecheck": "tsc --noEmit"
  }
}
